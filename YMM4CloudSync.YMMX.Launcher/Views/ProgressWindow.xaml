<Window x:Class="YMM4CloudSync.YMMX.Launcher.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YMM4CloudSync.YMMX.Launcher.Views"
        mc:Ignorable="d"
        Title="YMM4 Cloud Sync - 展開中" Height="150" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="StatusText"
                   Text="展開中..."
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <ProgressBar Grid.Row="1"
                     x:Name="Progress"
                     Height="20"
                     Minimum="0"
                     Maximum="100"
                     Value="0"/>

        <TextBlock Grid.Row="2"
                   x:Name="PercentText"
                   Text="0%"
                   HorizontalAlignment="Center"
                   Margin="0,5,0,0"/>
    </Grid>
</Window>
